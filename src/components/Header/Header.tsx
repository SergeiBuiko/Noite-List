import { Typography } from '@mui/material';
import React from 'react';

export const Header = () => {
  return (
    <Typography sx={{ fontSize: 30 }} variant="h2" component="h2">
      Note-list
    </Typography>
  );
};
